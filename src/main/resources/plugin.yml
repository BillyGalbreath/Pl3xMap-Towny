name: "${project.name}"
main: "${project.groupId}.Pl3xMapTowny"
version: "${project.version}"
description: "${project.description}"
api-version: "1.16"
authors: [ BillyGalbreath ]
softdepend:
  - Towny
  - Pl3xMap
